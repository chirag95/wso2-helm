apiVersion: v1
kind: Service
metadata:
  name: {{ include "wso-am.fullname" . }}
  labels:
    {{- include "wso-am.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wso-am.selectorLabels" . | nindent 4 }}
