apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wso-am.fullname" . }}-test-connection"
  labels:
    {{- include "wso-am.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wso-am.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
